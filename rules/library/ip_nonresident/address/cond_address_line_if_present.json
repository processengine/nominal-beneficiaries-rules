{
  "id": "library.ip_nonresident.address.cond_address_line_if_present",
  "type": "condition",
  "when": {
    "all": [
      "library.address.pred_full_address_present"
    ]
  },
  "steps": [
    "library.ip_nonresident.address.full_address"
  ]
}
