{
  "id": "internal.ul_resident.address.postal_code_text_if_present",
  "type": "condition",
  "when": "internal.ul_resident.address.postal_code_required",
  "steps": [
    "internal.ul_resident.address.postal_code_text"
  ]
}
